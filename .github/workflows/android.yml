name: CI
on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: ./
      - name: Use Node.jstest
        uses: actions/setup-node@v1
      - name: NPM test
        run: npm -v
